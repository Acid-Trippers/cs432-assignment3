"""
- Looks at initial_schema.json based on which it uses one of its data generations and chooses one of them for that particular field. 
- Sends the data as json after choosing all the field's datatypes.
- Uses the fieldname defined by the user as in initial_schema.json

Input: initial_schema.json
Output: None
"""